export type TExperience = {
  id: number;
  title: {
    rendered: string;
  };
  tags: number[];
  categories: number[];
  company: string;
  duration: string;
  location: string;
  description: string;
};
